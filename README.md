# investment-pipeline
a small end-to-end pipeline that ingests market/fund data, models it in a relational schema, runs analytical SQL and ETL transformations, and exposes an interactive dashboard summarizing performance
